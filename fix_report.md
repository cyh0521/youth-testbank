# Bug 修正紀錄

每次修正 bug 時，請在本檔最上方新增一筆，記錄日期、現象、原因、修正、影響檔案與實際驗證結果。若只完成語法或靜態檢查，應明確標示尚未驗證的操作流程。

## 2026-09-24 — 來源與題尾間距及試題對齊

- **現象**：預覽來源標記與題尾之間有多餘空格，試題文字只靠左對齊。
- **原因**：來源標記前的模板字串帶有空格，試題未指定左右對齊。
- **修正**：移除來源標記前的空格，恢復解析原有間距，並在預覽、列印、Word 與 PDF 的試題區套用左右對齊。
- **影響檔案**：`js/exam-preview.js`、`README.md`。
- **驗證**：`node --check js/exam-preview.js` 與 `git diff --check` 通過。
- **待驗證**：尚未在瀏覽器與 Word 中目視檢查長題目的左右對齊。

## 2026-09-24 — 預覽解析換行縮排

- **現象**：解析首行被整體縮排，長解析換行後未對齊「【解析】」後的第一個字。
- **原因**：解析區使用固定左邊距，標籤和內容仍在同一文字流中。
- **修正**：解析改為標籤與內文並排的版面，首行靠左、後續行在內文區換行；並將字型選項「宋體」改名「明體」。
- **影響檔案**：`js/exam-preview.js`、`README.md`。
- **驗證**：`node --check js/exam-preview.js` 與 `git diff --check` 通過。
- **待驗證**：尚未在瀏覽器中目視檢查長解析實際換行。

## 2026-09-24 — 預覽顯示答案時答題括號位移

- **現象**：切換是非題與選擇題的答案顯示時，括號內字元佔用寬度改變，使題號和題目位置跳動。
- **原因**：空白答案用兩個全形空格，有答案時則使用半形空格及答案字元，兩種內容寬度不同。
- **修正**：預覽答題括號改用固定寬度容器，答案置中；答案、解析與來源各用不同顏色辨識。
- **影響檔案**：`js/exam-preview.js`、`README.md`。
- **驗證**：`node --check js/exam-preview.js` 與 `git diff --check` 通過。
- **待驗證**：尚未在瀏覽器中目視確認開關切換時的位置與顏色。

## 2026-09-24 — 預覽解答與解析顯示位置不符

- **現象**：勾選預覽的解答或解析後，內容集中出現在試卷末尾，與參考試卷逐題顯示的格式不同；預覽選項也被帶入列印與下載。
- **原因**：預覽重用了輸出用的末尾解答區，且輸出按鈕傳入了預覽勾選狀態。
- **修正**：預覽改為將答案填入各題作答區、解析放在該題下方、來源接在題目末尾；新增「顯示來源」。列印、Word 與 PDF 不再讀取預覽勾選狀態。
- **影響檔案**：`js/exam-preview.js`、`README.md`。
- **驗證**：參照附件 `完整試卷.docx` 的題目、來源、解析段落順序；`node --check js/exam-preview.js` 與 `git diff --check` 通過。
- **待驗證**：尚未在登入後的瀏覽器中目視操作三個預覽開關與實際輸出。

## 2026-09-24 — 切換頁面時側欄短暫消失

- **現象**：點選側欄選項換頁時，側欄先全部消失，稍後才重新出現，感覺卡頓。
- **原因**：跨頁 View Transition 將舊頁的側欄快照設為 `display: none`，在新頁側欄可見前形成空白。
- **修正**：移除跨頁 View Transition 規則，保留各頁同步繪製側欄的流程。
- **影響檔案**：`css/theme.css`、`README.md`。
- **驗證**：確認過渡規則已移除，`git diff --check` 通過。
- **待驗證**：尚未在登入後的瀏覽器中實際切換各頁目視確認。

## 2026-09-24 — 進階設定出現重複層級

- **現象**：側邊欄進入設定頁後，頁面另顯示同名的「題庫環境設定」分頁與卡片標題，操作層級重複。
- **原因**：側邊欄拆分後，設定頁仍沿用原本的單一分頁結構與舊卡片標題。
- **修正**：進階設定頁直接顯示「選擇科目」、「版面設定」、「表頭設定」三個子項目；將「試卷邊距」放入版面設定，與字型、大小、行距共用儲存與重設操作；舊頁邊距深層連結導向版面設定。
- **影響檔案**：`settings.html`、`js/shell.js`、`README.md`。
- **驗證**：設定頁內嵌 JavaScript 語法檢查、`node --check js/shell.js` 與 `git diff --check` 通過；文字搜尋確認舊標籤及舊頁邊距按鈕已移除。
- **待驗證**：尚未在登入後的瀏覽器中目視檢查設定頁及實際操作儲存／重設。

## 2026-09-24 — Word 題號後空白過大與編輯記號

- **現象**：下載的 Word 試卷中，答題括號與題號後出現較大的空白及可見換行記號；先前也發生兩位數題號換行、多行題目的括號與首行錯位。
- **原因**：Word 將是非題、選擇題的 HTML 表格儲存格當作獨立排版區，欄寬及儲存格交界造成題號與題目分離。調整表格欄寬與頂端對齊只能處理部分症狀。
- **修正**：Word 匯出時，將每題的括號、題號及文字改為同一個懸掛縮排段落；網頁預覽、直接列印及 PDF 的題目版面維持原有表格定位。
- **影響檔案**：`js/exam-preview.js`；`README.md` 同步說明 Word 排版方式。
- **驗證**：以使用者提供的試卷文字在 Microsoft Word 開啟 20 題暫存測試檔，確認沒有題目表格、題號後沒有內部換行，且段落具備對應的一位數及兩位數題號懸掛縮排。`node --check js/exam-preview.js` 與 `git diff --check` 通過。
- **待驗證**：仍需從網站重新下載完整 Word 試卷，在 Word 中目視確認各頁的換行、題間距與解答區。

## 2026-09-25 — 進階設定變更影響已完成試卷

- **現象**：修改試卷表頭的共用設定後，已完成試卷的預覽與輸出也跟著變動。
- **原因**：試卷資料沒有保存自己的表頭，渲染時一律讀取瀏覽器的共用設定。
- **修正**：建立試卷時保存表頭；首次修改進階設定前，為尚無表頭資料的既有試卷保存當前設定。已完成試卷從預覽修改表頭時，只更新該試卷。
- **影響檔案**：`js/exam-preview.js`、`js/firebase.js`、`compose.html`、`manual.html`、`exams.html`、`settings.html`、`README.md`。
- **驗證**：JavaScript 語法與變更空白檢查。
- **待驗證**：需在登入狀態下，以既有試卷及新試卷實際檢查預覽、列印、Word 與 PDF；既有試卷若已被先前的設定變更影響，無法從現有資料還原更早的表頭。

## 2026-09-25 — 預覽表頭設定覆寫進階設定

- **現象**：在未儲存試卷的預覽視窗修改表頭時，共用的進階設定也被覆寫。
- **原因**：預覽表頭編輯將沒有試卷 ID 的草稿視為共用設定。
- **修正**：預覽表頭變更保存在目前試卷或草稿；草稿儲存為試卷時帶入該表頭。範例欄位取得焦點時隱藏提示，失焦且仍空白時恢復。
- **影響檔案**：`js/exam-preview.js`、`compose.html`、`manual.html`、`settings.html`、`css/theme.css`、`README.md`。
- **驗證**：JavaScript 語法與變更空白檢查。
- **待驗證**：需登入後確認草稿預覽、既有試卷及進階設定之間的表頭隔離。

## 2026-09-25 — 編輯試卷上方圖示消失

- **現象**：亂數排序、預覽與儲存按鈕只剩文字，試卷表頭仍有圖示。
- **原因**：按鈕使用新增的 `data-icon` 名稱，瀏覽器可能載入舊版圖示腳本，無法解析新名稱。
- **修正**：四個按鈕直接使用單色 SVG，避免依賴圖示腳本快取。
- **影響檔案**：`exams.html`。
- **驗證**：HTML 內四個按鈕均含 SVG；JavaScript 語法與變更空白檢查。
- **待驗證**：重新載入頁面後目視確認四個圖示均顯示。

## 2026-09-25 — Word 表頭第一列未顯示粗體

- **現象**：預覽表頭第一列為粗體，下載 Word 後未保持相同字重。
- **原因**：Word 對表頭容器上的 CSS `font-weight` 套用不穩定。
- **修正**：第一列欄位加入語意化 `<strong>`，並在 Word 樣式中明確指定粗體。試卷列表的卡片、資訊與操作按鈕圖示同步改為單色線條 SVG。
- **影響檔案**：`js/exam-preview.js`、`exams.html`。
- **驗證**：JavaScript 語法及變更空白檢查。
- **待驗證**：需在 Microsoft Word 實際開啟新下載的檔案，目視確認第一列表頭的字重。

## 2026-09-25 — Word 下載副檔名與實際格式不一致

- **現象**：原下載檔使用 `.doc`，實際內容是 HTML，無法直接當作真正的 `.docx`。
- **原因**：舊匯出流程將 HTML 字串以 `application/msword` 儲存。
- **修正**：改用 docx.js 組成 OOXML 文件並下載 `.docx`；保留紙張尺寸、邊距、單雙欄、表頭、題目及解答。班級、姓名、座號格數預設改為 8、8、0，選單限制在 0–12 格。
- **影響檔案**：`js/exam-preview.js`、`js/docx-export.js`、`settings.html`、`vendor/docx.iife.js`、`vendor/docx.LICENSE`、`README.md`。
- **驗證**：以瀏覽器產生單欄及雙欄測試 DOCX，確認為有效 ZIP／OOXML，包含表頭粗體、JIS B4 頁面尺寸與雙欄 section；可由 python-docx 開啟。從網站的 Word 匯出函式進行整合測試，下載檔名為 `.docx`，內容包含題目、三個 section 與粗體表頭。JavaScript 語法與變更空白檢查通過。
- **待驗證**：需用真實試卷在 Microsoft Word 目視檢查字型、換行、分頁與圖片；遠端圖片若不允許瀏覽器擷取，會退回替代文字。

## 2026-09-25 — DOCX 在 Word 中題距過大並顯示黑色方塊

- **現象**：Word 開啟新匯出的 DOCX 時，題目與表頭間距被拉大，段落左側出現黑色方塊。
- **原因**：東亞文字貼齊 Word 文件格線，將指定行高向上取整；`keepNext` 與 `keepLines` 段落屬性在顯示格式標記時呈現黑色方塊。
- **修正**：每個文字 run 明確指定東亞字型並關閉貼齊格線；移除表頭、題型標題與題目的上述段落限制。
- **影響檔案**：`js/docx-export.js`。
- **驗證**：從網站匯出流程產生測試 DOCX；檢查 OOXML 中沒有 `keepNext`／`keepLines`，所有文字 run 都含 `snapToGrid=false`，且東亞字型一致；python-docx 可開啟。
- **待驗證**：此執行環境無法啟動 Word COM，需在 Word 中重新下載並目視確認實際行距與分頁。

## 2026-09-25 — Word 字型與行距仍不同於預覽

- **現象**：使用者提供的 DOCX 在 Word 中，字型及行距仍與網站預覽不同。
- **原因**：匯出將預覽的 Noto Sans TC 換成微軟正黑體；行距規則輸出為 `exactly`，不是 Word 使用的 `exact`。
- **修正**：匯出沿用預覽的 Noto Sans TC／Noto Serif TC 字型；行距改用 Word 可辨識的 `exact` 規則。
- **影響檔案**：`js/docx-export.js`。
- **驗證**：以 16px、1.85 行距產生 DOCX，確認 OOXML 寫入 Noto Sans TC、444 twips 與 `lineRule=exact`，且 python-docx 可解析行距。
- **待驗證**：需於 Microsoft Word 開啟重新下載的實際試卷，確認視覺效果。

## 2026-09-25 — Word 解答標題與表頭分隔線、表頭設定儲存行為

- **現象**：Word 的「【解答與解析】」未加粗且缺少題目與解答間的分隔線；表頭第二列與底線距離不同於預覽；儲存試卷表頭後設定區未關閉。
- **原因**：DOCX 匯出未轉換解答區標題的粗體與上方虛線，也未設定表頭底線到文字的距離；儲存處理只更新狀態文字。
- **修正**：解答標題套用粗體與虛線上框；表頭第二列底線加入 8pt 間距；儲存成功後關閉表頭設定區。
- **影響檔案**：`js/docx-export.js`、`js/exam-preview.js`。
- **驗證**：產生測試 DOCX，確認解答標題粗體、虛線上框及表頭底框間距均寫入 OOXML，且 python-docx 可開啟；JavaScript 語法與變更空白檢查通過。
- **待驗證**：需在 Microsoft Word 目視確認線條和間距的實際呈現。

## 2026-09-25 — 試卷行距與個別外觀設定

- **現象**：調整預覽中的字體與行距會覆寫進階設定的預設值，其他試卷的預覽與匯出隨之改變；表頭行距也跟著題目行距變動。
- **原因**：預覽與匯出都讀寫同一份瀏覽器外觀設定，表頭繼承試卷行距。
- **修正**：預設題目行距改為 1.3，表頭固定 1.6；各試卷獨立保存外觀，預覽、列印、Word、PDF 使用試卷外觀。進階設定只作為新試卷預設值，變更前先為尚無外觀資料的既有試卷保存原值。
- **影響檔案**：`js/exam-preview.js`、`js/docx-export.js`、`js/firebase.js`、`settings.html`、`compose.html`、`manual.html`、`coded.html`、`exams.html`、`README.md`。
- **驗證**：JavaScript 語法、試卷建立與匯出路徑檢查；DOCX 行距寫入檢查。
- **待驗證**：在網站以多張真實試卷確認跨頁面資料保存及 Word 視覺效果。

## 後續記錄格式

```md
## YYYY-MM-DD — 問題名稱

- **現象**：使用者看到了什麼、如何重現。
- **原因**：確認過的根因；若尚未確認，請標示推測。
- **修正**：實際改動及其作用。
- **影響檔案**：檔案路徑。
- **驗證**：執行過的檢查、測試或實際操作結果。
- **待驗證**：尚未完成的驗證或已知限制；沒有則寫「無」。
```
